apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
data:
  hasuraSecret: {{ .Values.hasuraSecret | quote }}
  jwtSecret: {{ .Values.jwtSecret | quote }}
  flaskSecret: {{ .Values.flaskSecret | quote }}
  passwordSalt: {{ .Values.passwordSalt | quote }}
