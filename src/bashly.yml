name: artemis
help: ARTEMIS installer script
version: 0.1.0

args:
- name: action
  required: true
  help: the action to perform

flags:
- long: --build
  short: -b
  help: Build the images from the Dockerfiles

examples:
- artemis install
- artemis install -b
